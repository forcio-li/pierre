import './Hero.scss';

import { Navigation } from '../../components';

export const Hero = () => {
  return (
    <section id="hero" className="section-hero">
      <Navigation />
    </section>
  );
};
